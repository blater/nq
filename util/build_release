#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cat >&2 <<'NOTICE'
util/build_release now uses the cross-platform GitHub Actions release path so
Windows and Chocolatey artifacts cannot be skipped.
NOTICE

exec "$script_dir/release.sh" "$@"
